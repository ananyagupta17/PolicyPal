services:
  - type: web
    name: policy-ml-service
    env: python
    rootDir: ml
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn ml.main:app --host 0.0.0.0 --port 8000
    autoDeploy: true
